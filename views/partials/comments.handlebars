<div class="comments li-group">
  {{#each this}}
  <section class="comment list-item">
    <div class="meta">
     <h3> {{user.username}} on {{format_date created_at}}</h3>
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>